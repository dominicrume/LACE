# Stage: Deterministic Rules (final say)
## Input: item metadata + optional vision suggestion
## Process: execute_triage(). WEEE -> safety queue, requires_pat, ALWAYS.
Then circular hierarchy: reuse > repair > salvage. Loud failure on bad input.
## Output: decision with justification (provenance, Rule 10)
## Completion: persist decision; operator may override (recorded), except
WEEE re-routing before PAT (409).
